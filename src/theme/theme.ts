export enum Spacing {
  XXS = '4px',
  S = '8px',
  M = '12px',
  M_NUDE = '14px',
  L = '16px',
  XL = '24px',
  XXL = '26px',
}

export enum BorderWidth {
  Small = '1px',
  Medium = '2px',
}

export enum BorderRadius {
  Circular = '1000px',
  S = '4px',
  M = '6px',
  XL = '14px',
}

export enum Duration {
  FastInteraction = 100,
  DefaultInteraction = 250,
}

export enum Font {
  Default = 'Roboto Flex, sans-serif',
}

export enum LineHeight {
  XXS = 16,
  S = 20,
  M = 22,
}

export enum FontSize {
  S = 14,
  M = 16,
}
